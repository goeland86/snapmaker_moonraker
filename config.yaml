server:
  host: "0.0.0.0"
  port: 7125

printer:
  ip: ""          # Snapmaker J1S IP address (required)
  token: ""       # Authentication token (confirmed at printer HMI)
  model: "Snapmaker J1S"
  poll_interval: 2  # Status poll interval in seconds

files:
  gcode_dir: "gcodes"  # Local directory for gcode file storage
